apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: website
spec:
  image: ghcr.io/thdxg/website
  interval: 5m
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: website
spec:
  imageRepositoryRef:
    name: website
  filterTags:
    pattern: '^\d{8}-\d{6}-[a-f0-9]+'
    extract: "$0"
  policy:
    alphabetical:
      order: asc
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageUpdateAutomation
metadata:
  name: website
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        name: flux
        email: flux@thdxg.dev
    push:
      branch: main
  update:
    path: ./apps/website
    strategy: Setters
