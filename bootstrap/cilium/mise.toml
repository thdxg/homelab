[tasks.install]
description = "Install Cilium Gateway API CRDs"
run = """
# Gateway API CRDs pinned to what Envoy Gateway (apps/envoy-gateway) is
# compiled against — EG v1.8 → v1.5.1. Cilium's own gateway support is
# disabled (see values.yaml). Never use the unpinned "latest" URL — CRD
# skew against controllers causes subtle breakage.
kubectl apply --server-side -f https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.5.1/experimental-install.yaml
cilium install --version 1.20.0 \
  --values values.yaml \
  --set k8sServiceHost={{ env.NODE_1_IP_ETH }} \
  --set k8sServicePort=6443 \
  --set ipv4NativeRoutingCIDR={{ env.CLUSTER_CIDR }}
cilium status --wait
"""

[tasks.test]
description = "Test Cilium connectivity"
run = """
cilium connectivity test --test-concurrency 5
kubectl delete namespace cilium-test-1 cilium-test-ccnp1 cilium-test-ccnp2
"""
